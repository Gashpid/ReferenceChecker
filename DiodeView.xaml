<UserControl x:Class="ReferenceChecker.DiodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:ReferenceChecker"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Button Content="Home" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Click="HomeButton_Click"/>
        <TextBlock Name="TitleTextBlock" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,30,0,0" FontSize="20" FontWeight="Bold"/>

        <lvc:CartesianChart Name="DiodeChart" VerticalAlignment="Center" HorizontalAlignment="Center" Width="400" Height="300"/>
    </Grid>
</UserControl>
